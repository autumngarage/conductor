# PR #106 merged - harden Claude exec startup

**Date:** 2026-04-28
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** GitHub issues #104 and #105
**Merge-commit:** a50c8bb35db0c6d498c8cba08ea78d8b99840ced
**Branch:** fix/claude-exec-stalls-permissions

> Conductor now fails Claude exec sessions earlier when the nested CLI never
> emits an initial byte, and nested Claude Code runs load project/local settings
> deterministically for repo-scoped workspace-write dispatches.

## What shipped

- Claude exec now has a first-output watchdog that is distinct from the
  existing mid-task stall watchdog. It emits structured `error` data with
  `phase: first_output` and `last_event: provider_started`.
- The first-output watchdog respects `--max-stall-seconds 0` by disabling
  itself when the normal stall watchdog is disabled.
- Claude exec now passes `--setting-sources user,project,local` when `--cwd`
  is supplied, sets child `PWD` to the effective cwd, and validates/logs
  `.claude/settings.json` plus `.claude/settings.local.json` diagnostics for
  workspace-write runs.
- Regression coverage pins first-output stalls, watchdog-disable behavior,
  project settings propagation, and invalid project settings preflight.

## Closes / advances

- **Plans:** none.
- **Doctrine:** none.
- **Journal linkage:** this merge closes GitHub issues #104 and #105.

## Follow-ups (deferred to future work)

None.
