# PR #552 merged — fix: harden gemini call defaults

**Date:** 2026-06-09
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** issue/519
**Merge-commit:** f5893683ff8e93bb7665343279f6184b81a98baa
**Branch:** fix/issue-519-gemini-headless-call

> Shipped the remaining Conductor-side reliability fixes for Gemini headless `call` usage and closed issue #519.

## What shipped

- Gemini CLI `call` mode now gets a concrete 540s default timeout when the user leaves `--timeout` unset, and that default flows through the existing slow-network scaling path.
- Explicit `--timeout` values for Gemini calls remain exact overrides and are not scaled.
- Semantic `ask` call-mode dispatch applies the same Gemini timeout policy when routing to Gemini.
- Gemini call-mode prompts that ask for web source URLs or citations now emit a warning that Conductor does not verify provider-generated external links.
- CLI regressions cover pinned Gemini timeout scaling, explicit timeout overrides, the citation warning, and semantic text-review routing to Gemini.

## Closes / advances

- **Plans:** none.
- **Doctrine:** none.
- **Journal linkage:** This entry ratifies PR #552 and the fix for issue #519.

## Follow-ups (deferred to future work)

None.

(Per SPEC § 4.2, deferred items must resolve to another Plan, Journal entry, or Doctrine entry in the same commit as the merge note.)
