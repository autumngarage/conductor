# Secrets & config (NEVER commit these)
.env
.env.*
!.env.example
# .envrc untracked because `conductor init` may append API keys to it.
# Track .envrc.example as the source-of-truth template; copy to .envrc and
# `direnv allow` to opt in.
.envrc
!.envrc.example
config.json
credentials.json
*.pem
*.key
*.p12

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Dependencies
node_modules/
vendor/
__pycache__/
*.pyc
.venv/
venv/

# Build output
dist/
build/
*.egg-info/
.next/
out/

# Touchstone
*.bak
.cortex/.index.json
.cortex/pending/
src/conductor/_version.py
.cortex/.index/
